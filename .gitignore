__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
data/
*.pem
.pytest_cache/
.uv-cache/
.uv/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.DS_Store
Thumbs.db

# Claude Code per-user settings (MCP allowlist etc.) — personal preference,
# do not version-control.
.claude/settings.local.json

# Per-machine MCP config — paths to `charter-mcp` binary and `data/` dir
# vary by user. The template lives at `.mcp.json.example`.
.mcp.json

# Top-level hackathon-era design doc lives at `docs/legacy/hackathon-design.md`
# now. The old top-level copy stays local-only.
Charter-黑客松项目文档.md
